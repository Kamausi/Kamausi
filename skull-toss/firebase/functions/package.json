{
  "name": "skull-toss-functions",
  "private": true,
  "description": "Skull Toss's server: Souls, the validated leaderboard, remote config and consent-gated analytics.",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": { "test": "node --test test/*.test.js", "serve": "firebase emulators:start --only functions,firestore,auth", "deploy": "firebase deploy --only functions,firestore" },
  "dependencies": { "firebase-admin": "^12.1.0", "firebase-functions": "^5.0.1" }
}
